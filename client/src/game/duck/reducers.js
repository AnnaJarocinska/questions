import types from './types';

const INITIAL_STATE = {

  points: 0,
}

const gameReducer = (state = INITIAL_STATE, action) => {

    switch (action.type) {
      default:
        return state   
    }
  }

export default gameReducer;