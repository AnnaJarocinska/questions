import React from 'react';

const RealGameDashboard = ({categories, mode}) => {
    return (
        <>
        <p>{categories}</p>
        <p>{mode}</p>
        </>
      );
}
 
export default RealGameDashboard;